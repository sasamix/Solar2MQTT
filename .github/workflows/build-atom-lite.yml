name: Build M5Stack ATOM Lite

on:
  push:
    branches: [ main ]
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: write

# Prevent parallel pushes from racing while replacing release assets.
concurrency:
  group: atom-lite-release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Install PlatformIO
        run: pip install platformio
      - name: Build ATOM Lite
        run: pio run -e m5stack_atom_lite
      - name: Verify OTA artifact
        run: |
          test -s ".firmware/Solar2MQTT_m5stack_atom_lite_V0.0.0-dev-2.0.11.bin.ota"
          test -s ".firmware/Solar2MQTT_m5stack_atom_lite_V0.0.0-dev-2.0.11.bin"
          ls -lh .firmware/
      - name: Upload firmware to GitHub Release
        if: github.event_name == 'release' || github.event_name == 'push'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${{ github.event_name == 'release' && github.event.release.tag_name || 'v0.0.0-dev-2.0.11' }}"
          # Create the versioned release on first publication.
          gh release view "$TAG" >/dev/null 2>&1 || gh release create "$TAG" --title "Solar2MQTT 0.0.0-dev-2.0.11" --target "${{ github.sha }}" --notes "M5Stack ATOM Lite diagnostic build 0.0.0-dev-2.0.11"
          # Delete by asset name first. This avoids gh --clobber using a stale asset ID.
          gh release delete-asset "$TAG" "Solar2MQTT_m5stack_atom_lite_V0.0.0-dev-2.0.11.bin.ota" -y 2>/dev/null || true
          gh release delete-asset "$TAG" "Solar2MQTT_m5stack_atom_lite_V0.0.0-dev-2.0.11.bin" -y 2>/dev/null || true
          gh release upload "$TAG" \
            ".firmware/Solar2MQTT_m5stack_atom_lite_V0.0.0-dev-2.0.11.bin.ota" \
            ".firmware/Solar2MQTT_m5stack_atom_lite_V0.0.0-dev-2.0.11.bin"
      - uses: actions/upload-artifact@v4
        with:
          name: Solar2MQTT-m5stack-atom-lite
          path: |
            .firmware/Solar2MQTT_m5stack_atom_lite_V0.0.0-dev-2.0.11.bin.ota
            .firmware/Solar2MQTT_m5stack_atom_lite_V0.0.0-dev-2.0.11.bin
          if-no-files-found: error
